using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class parameters : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
}
//Each Parameter needs to have descriptive names.
//a method can recieve data by specifying a parameter, and then can send it back by putting a return function.
//parameters read and record what is listed by name.